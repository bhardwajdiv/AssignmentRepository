<?xml version="1.0" encoding="UTF-8"?>
<screens xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns="http://ofbiz.apache.org/Widget-Screen"
    xsi:schemaLocation="http://ofbiz.apache.org/Widget-Screen
        http://ofbiz.apache.org/dtds/widget-screen.xsd">

    <!-- ═══════════════════════════════════════════════════════════════
         Common decorator: OFBiz app chrome
         ═══════════════════════════════════════════════════════════════ -->
    <screen name="main-decorator">
        <section>
            <actions>
                <set field="layoutSettings.companyName"     from-field="uiLabelMap.CustomerManagementCompanyName"/>
                <set field="layoutSettings.companySubtitle" from-field="uiLabelMap.CustomerManagementCompanySubtitle"/>
                <set field="activeApp"                      value="CustomerManagement"/>
                <set field="applicationMenuName"            value="AppBarMenu"/>
                <set field="applicationMenuLocation"
                    value="component://CustomerManagement/widget/CustomerManagementMenus.xml"/>
                <set field="applicationTitle"               value="${uiLabelMap.CustomerManagementApplication}"/>
            </actions>
            <widgets>
                <include-screen name="ApplicationDecorator"
                    location="component://commonext/widget/CommonScreens.xml"/>
            </widgets>
        </section>
    </screen>

    <!-- ═══════════════════════════════════════════════════════════════
         main – landing page
         ═══════════════════════════════════════════════════════════════ -->
    <screen name="main">
        <section>
            <actions>
                <set field="headerItem" value="main"/>
            </actions>
            <widgets>
                <decorator-screen name="main-decorator"
                    location="${parameters.mainDecoratorLocation}">
                    <decorator-section name="body">
                        <screenlet title="${uiLabelMap.CustomerManagementApplication}">
                            <label text="Welcome to Customer Management. Use the menu above to search for customers."/>
                        </screenlet>
                    </decorator-section>
                </decorator-screen>
            </widgets>
        </section>
    </screen>

    <!-- ═══════════════════════════════════════════════════════════════
         FindCustomer – Step 8: renders findCustomer.ftl via
         platform-specific/html-template.  Actions run FindCustomer.groovy
         which calls findCustomer service and handles pagination.
         ═══════════════════════════════════════════════════════════════ -->
    <screen name="FindCustomer">
        <section>
            <actions>
                <set field="headerItem"    value="FindCustomer"/>
                <set field="titleProperty" value="PageTitleFindCustomer"/>
                <script location="component://CustomerManagement/webapp/CustomerManagement/WEB-INF/actions/FindCustomer.groovy"/>
            </actions>
            <widgets>
                <decorator-screen name="main-decorator"
                    location="${parameters.mainDecoratorLocation}">
                    <decorator-section name="body">
                        <platform-specific>
                            <html>
                                <html-template
                                    location="component://CustomerManagement/webapp/CustomerManagement/findCustomer.ftl"/>
                            </html>
                        </platform-specific>
                    </decorator-section>
                </decorator-screen>
            </widgets>
        </section>
    </screen>

</screens>
